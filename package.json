{
  "name": "tj.kanda.la",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsc && node dist/build",
    "dev": "npm run build && npx serve build"
  },
  "dependencies": {
    "@types/node": "^14.0.24",
    "showdown": "^1.9.1",
    "typescript": "^3.9.6"
  },
  "devDependencies": {
    "@types/showdown": "^1.9.3"
  }
}
